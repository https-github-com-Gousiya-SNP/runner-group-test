{
  "name": "my-node-project",
  "version": "1.0.0",
  "description": "A simple Node.js project",
  "main": "src/index.js",
  "scripts": {
    "build": "echo 'Building project...' && mkdir -p build && cp -r src/* build/"
  },
  "dependencies": {}
}
